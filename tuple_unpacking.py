# Define a tuple containing the dimensions of a rectangle
dimensions=(5,4)#here 5 is length and 3 is width
#we are doing tuple unpacking into 2 variables
length,width=dimensions
print(f"The length of rectangle is {length} and its width is{width}than the area of rectangle is{length*width}")
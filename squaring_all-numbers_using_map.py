#using map we are squaring all elements in list
result=map(lambda i:i**2,[1,2,3,4,5])#syntax of lambda is lambda arugumenrt:expression,values
print(list(result))#if we dont do type conversion it prints object